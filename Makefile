# Makefile for Vue.js + FastAPI Project

# Variables
FRONTEND_DIR := frontend
BACKEND_DIR := backend

VENV_DIR := .venv
PYTHON := $(VENV_DIR)/

FRONTEND_PORT := 5173
BACKEND_PORT := 8000
DOCKER_COMPOSE_FILE := docker-compose.yml
